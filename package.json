{
  "name": "moat-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Moat — Chrome extension and its marketing site.",
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "build": "pnpm -r build",
    "test": "pnpm --filter moat test",
    "type-check": "pnpm --filter moat type-check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
